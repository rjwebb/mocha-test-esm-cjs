{
  "exclude": ["dist"],
  "extends": "../../../tsconfig.base.json", // Extend the base configuration
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "dist"
  },
  "references": [
    { "path": "../." },
    { "path": "../../other-project-cjs" }
  ]
}
